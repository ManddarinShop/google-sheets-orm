/** Numeric value used when validating that a string is empty. */
export const EMPTY_STRING_LENGTH_ZERO = 0 as const;

/** Numeric value used when validating that an array is empty. */
export const EMPTY_ARRAY_LENGTH_ZERO = 0 as const;

/** Number of rows expected when one guarded storage mutation succeeds. */
export const EXPECTED_SINGLE_ROW_CHANGE_COUNT = 1 as const;
